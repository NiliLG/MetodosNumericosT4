Función integralSimpson38(función, a, b)
    h = (b - a) / 3 // Ancho de cada subintervalo
    x0 = a
    x1 = a + h
    x2 = a + 2 * h
    x3 = b
    
    sum = función(x0) + 3 * función(x1) + 3 * función(x2) + función(x3)
    
    retornar (b - a) * sum / 8
